using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SenseeOne : MonoBehaviour
{
    /*public bool isObjectAttached;
    public bool ishandAttached;
    public int  objectId;
    public int  boneId;
    public Vector3 position;
    private void OnTriggerEnter(Collider col)
    {
        if(col.gameObject.CompareTag("HandModel"))
        {
            isObjectAttached = false;
            ishandAttached = true;
            objectId = 0;
            //boneId
            position = gameObject.transform.position;
            
        }

        if (col.gameObject.CompareTag("spawnedObject"))
        {
            isObjectAttached = true;
            ishandAttached = false;
            objectId = col.gameObject.GetInstanceID();
            boneId = 0;
            position = gameObject.transform.position;
        }

    }*/
}
